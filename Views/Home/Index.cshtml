@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Crie uma nova conta <a href="@Url.Action("Register","Account")"> Aqui</a>.</p>
    <p><a href="@Url.Action("Login","Account")"> Login</a>.</p>
</div>
